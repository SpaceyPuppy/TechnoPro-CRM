{
  "name": "technopro-crm",
  "version": "0.1.0",
  "private": true,
  "description": "Internal CRM + POS for tech repair shops",
  "workspaces": [
    "backend",
    "web",
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "backend:dev": "npm run dev --workspace=backend",
    "web:dev": "npm run dev --workspace=web",
    "mvp:check": "npm run build --workspace=@technopro/shared && npm run build --workspace=backend && npm test --workspace=backend"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
